// Move to LocalStorage Service
const saveToken = (token) => {
  localStorage.setItem(token)
}

const authWithFirebase = ({ email, password }) => {
  // const auth = firebase.authWithEmail(email,password)
  // saveToken(auth);
  // return true || false  
}